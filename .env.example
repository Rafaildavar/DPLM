# =============================================================================
# Конфигурация подключения к БД для приложения DPLM.
# Скопируйте этот файл в `.env` и при необходимости переопределите значения.
# Файл `.env` подгружается автоматически в app/models/database.py.
# =============================================================================

# Полный SQLAlchemy URL (имеет наивысший приоритет, если задан).
# DATABASE_URL=postgresql+psycopg2://dplm:dplm@localhost:5432/dplm

# Параметры PostgreSQL (используются, если DATABASE_URL не задан).
DPLM_DB_BACKEND=postgres
DPLM_DB_HOST=localhost
DPLM_DB_PORT=5433
DPLM_DB_USER=dplm
DPLM_DB_PASSWORD=dplm
DPLM_DB_NAME=dplm

# Резервный SQLite-файл (используется при DPLM_DB_BACKEND=sqlite или тестах).
DPLM_SQLITE_PATH=.db_data/dplm_dev.sqlite
