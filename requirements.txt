# Computer Vision (headless on macOS to avoid Qt/GUI conflicts)
opencv-python-headless
# С 0.10.33 модуль mp.solutions удалён — весь проект использует Tasks API
# (см. cv/hand_landmarker.py). Минимальная версия выровнена под это.
mediapipe>=0.10.33
numpy
Pillow>=10.0

# Machine Learning
scikit-learn
mlflow
psutil
torch
optuna

# Voice Assistant
pyttsx3
SpeechRecognition
vosk

# GUI Framework — Flet (Flutter desktop runtime, без Qt-плагинов).
# Точка входа: `python -m app.flet_app.main`. На macOS 26 (Tahoe) + Apple
# Silicon работает «из коробки», т. к. не использует системные Qt/Cocoa
# плагины (которые ломались у PySide6 из-за новой Gatekeeper-проверки
# `com.apple.provenance` и `@rpath/QtGui.framework`).
flet>=0.85.3
flet-desktop>=0.85.3

# Legacy QML/PySide6 (старая ветка `app/main.py` + 22 QML-файла).
# Не нужен для Flet-версии. Раскомментируй, если хочешь сравнить с QML;
# фиксированная версия 6.9.3 — последняя, которая на macOS 26 работала.
# PySide6==6.9.3
# PySide6-Addons==6.9.3
# PySide6-Essentials==6.9.3
# shiboken6==6.9.3

# Database
# pg_embed — пакет отсутствует на PyPI; приложение использует SQLite fallback (см. app/models/database.py)
psycopg2-binary
sqlalchemy
alembic

# Command Execution
pyautogui

# Development
joblib

# Testing
pytest
pytest-cov
pytest-mock
# pytest-qt — нужен только для тестов legacy QML/PySide6 версии.
# pytest-qt
