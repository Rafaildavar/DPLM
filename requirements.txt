# Computer Vision (headless build avoids GUI/Qt conflicts)
opencv-python-headless
# С 0.10.33 модуль mp.solutions удалён — весь проект использует Tasks API
# (см. cv/hand_landmarker.py). Минимальная версия выровнена под это.
mediapipe>=0.10.33
numpy
Pillow>=10.0

# Machine Learning
scikit-learn
mlflow
psutil
torch
optuna
tensorflow

# Voice Assistant
pyttsx3
SpeechRecognition
vosk

# GUI Framework — Flet (Flutter desktop runtime).
# Точка входа: `python -m app.flet_app.main`.
flet>=0.85.3
flet-desktop>=0.85.3

# Database
# pg_embed — пакет отсутствует на PyPI; приложение использует SQLite fallback (см. app/models/database.py)
psycopg2-binary
sqlalchemy
alembic

# Command Execution
pyautogui

# Development
joblib

# Testing
pytest
pytest-cov
pytest-mock
